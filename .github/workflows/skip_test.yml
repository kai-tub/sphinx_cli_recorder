name: Test
on:
  push:
    branches: [main]
    paths-ignore:
      - "sphinx_cli_recorder/**"
  pull_request:
    branches: [main]
    paths-ignore:
      - "sphinx_cli_recorder/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
